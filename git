.git status
.git add
.git status -s
.git diff
    changed but not yet staged. working area vs. staging area
    before add vs. after add
git diff --staged # staged changes vs. last commit
git diff --cached

nochanged->(edit)->changed->(add)->staged->(commit)->(push)->remote repository
git diff # nochanged vs. changed
git diff --cached
git commit
git commit -v
git commit -m
git commit -a # skip git add
git rm # remove staged area and working area both
    #git unset HEAD <filename>
    #git checkout <filename>
git rm --cached# remove staged area and working area both
    #git checkout <filename>

git mv
    git rm
    git add
